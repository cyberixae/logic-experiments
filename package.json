{
  "name": "prop",
  "version": "0.0.1",
  "description": "Propositional logic stuff",
  "main": "index.js",
  "license": "UNLICENSED",
  "author": "Toni Ruottu <toni.ruottu@iki.fi>",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/jest": "^29.5.12",
    "esbuild": "^0.27.3",
    "eslint": "^10.0.3",
    "jest": "^29.7.0",
    "prettier": "^3.2.5",
    "ts-jest": "^29.1.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.57.1"
  },
  "scripts": {
    "build:web": "yarn tsc --project tsconfig.web.json && esbuild --bundle --format=esm src/web.ts --outfile=dist/lk.js && esbuild --bundle src/web/challenge-worker.ts --outfile=dist/lk.w.js",
    "dev": "esbuild --bundle --format=esm src/web.ts --outfile=dist/lk.js --watch=forever & esbuild --bundle src/web/challenge-worker.ts --outfile=dist/lk.w.js --watch=forever & esbuild --servedir=dist & open http://localhost:8000/lk.html && wait",
    "typecheck": "yarn tsc --noEmit",
    "test": "yarn jest",
    "build": "yarn tsc",
    "lk": "yarn build && node ./lib/lk.js",
    "la3": "yarn build && node ./lib/la3.js",
    "main": "yarn build && node ./lib/main.js",
    "namegen": "node scripts/namegen.ts",
    "prettify": "prettier . --write"
  }
}
